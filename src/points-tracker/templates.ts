const tplPointsTracker = (
  gamedatas: GamedatasAlias
) => `<div id="fott-points-tracker">
  <div id="fott-points-tracker-dial"></div>
</div>`;

const tplAnimalTokenSpot = (id: string) => `<div id="${id}" class="fott-animal-token-spot"></div>`;